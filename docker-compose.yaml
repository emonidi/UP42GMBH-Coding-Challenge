version: '3.7'

services:

  weatherapp:
    image: emonidi/weatherapp:latest
    container_name: weatherapp
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 80:80
